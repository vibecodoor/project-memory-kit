# {{PROJECT_NAME}} — Journal

<!-- DEV MEMORY KIT · append-only problem→solution log. Lazy: read when "have we hit this before?".
     Born on the first non-trivial problem worth not repeating. One entry = one problem.
     Signal only — skip trivial fixes. Prune once the lesson is folded into PROJECT/DECISIONS or goes stale.
     Newest first. Keep it dense, not a diary. -->

### {{YYYY-MM-DD}} — {{problem in one line}}

- **Problem:** {{symptom + context — what broke / blocked, and where}}
- **Tried & rejected:** {{approaches that failed + why — the dead ends}}
- **Fix:** {{what actually worked}}
- **Lesson:** {{durable takeaway — what to do / avoid next time}}
